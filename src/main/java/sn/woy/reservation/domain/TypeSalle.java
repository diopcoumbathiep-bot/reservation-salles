package sn.woy.reservation.domain;

/** Type d'usage prévu pour une salle (ensemble fermé de valeurs). */
public enum TypeSalle {
    SALLE_COURS,
    SALLE_TP,
    SALLE_REUNION,
    AMPHITHEATRE,
    SALLE_SOUTENANCE
}
